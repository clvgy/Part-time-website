<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clvgy.system.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.clvgy.model.system.SysUser" autoMapping="true">
    </resultMap>
    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
      id,username,password,phone,head_url,status,create_time,update_time,is_deleted
   </sql>

    <select id="selectPage" resultMap="SysUserMap">
        select <include refid="columns" />
        from sys_user
        <where>
            <if test="vo.keyword != null and vo.keyword != ''">
                and (username like CONCAT('%',#{vo.keyword},'%') or phone like CONCAT('%',#{vo.keyword},'%'))
            </if>
            <if test="vo.createTimeBegin != null and vo.createTimeBegin != ''">
                and create_time >= #{vo.createTimeBegin}
            </if>
            <if test="vo.createTimeEnd != null and vo.createTimeEnd != ''">
                and create_time &lt;= #{vo.createTimeEnd}
            </if>
            and is_deleted = 0
        </where>
        order by id desc
    </select>

    <select id="findSchoolUser" resultMap="SysUserMap">
        select <include refid="columns" />
        from sys_user
        <where>
            id in (select user_id from sys_user_role where role_id = 4)
        </where>
        order by id desc
    </select>
    <select id="findEnterpriseUser" resultType="com.clvgy.model.system.SysUser">
        select <include refid="columns" />
        from sys_user
        <where>
            id in (select user_id from sys_user_role where role_id = 2)
        </where>
        order by id desc
    </select>

    <select id="selectStudentPage" resultType="com.clvgy.model.system.SysUser">
        select <include refid="columns" />
        from sys_user
        <where>
            id
            IN(SELECT re.user_id FROM sys_resume re WHERE re.school_id
            IN(SELECT id FROM sys_school sc WHERE sc.user_id
            IN(SELECT us.id FROM sys_user us WHERE us.username=#{username})))
            <if test="vo.keyword != null and vo.keyword != ''">
                and (username like CONCAT('%',#{vo.keyword},'%') or phone like CONCAT('%',#{vo.keyword},'%'))
            </if>
            and is_deleted = 0
        </where>
        order by id desc
    </select>


</mapper>
